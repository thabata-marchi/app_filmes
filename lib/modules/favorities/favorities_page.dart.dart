import 'package:flutter/material.dart';
    
class FavoritiesPageDart extends StatelessWidget {

  const FavoritiesPageDart({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Favorities'),
      ),
      body: Container(),
    );
  }
}